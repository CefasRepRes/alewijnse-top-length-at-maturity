# M2
# length ~ sex + dd + age

model {

  # priors
  intercept ~ dnorm(0, 0.001)
  beta_sex[1] = 0
  beta_sex[2] ~ dnorm(0, 0.001)
  beta_dd ~ dnorm(0, 0.001)
  beta_age ~ dnorm(0, 0.001)
  tau ~ dgamma(0.01, 0.01)

  # likelihood
  for (i in 1:n) {
    lp[i] = intercept + beta_sex[sex[i]] + beta_dd * dd[i] + beta_age * age[i]
    length[i] ~ dnorm(lp[i], tau)

    # log likelihood
  loglik[i] <- logdensity.norm(length[i], lp[i], tau)
  }
}
